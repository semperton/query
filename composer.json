{
    "name": "semperton/query",
    "description": "A small and standalone SQL query builder.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=7.2"
    },
    "autoload": {
        "psr-4": {
            "Semperton\\Query\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit tests",
        "check": "psalm"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5",
        "vimeo/psalm": "^4.7",
        "semperton/search": "^0.2.0"
    }
}
